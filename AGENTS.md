# MASTER — Mac & Business Continued in Codex

This repository is a subordinate Dealflows runtime-support component governed by the canonical portfolio control plane in `davidligotti/dealflows-lead-gen` issue **#115**.

## Authority
- **Autonomy Runner** owns the canonical Work ID, duplicate key, lifecycle, lease, dependencies, resource locks, cost ceiling, evidence, settlement, and next safe action.
- The pinned Mac Codex conversation **“MASTER — Mac & Business Continued”** is the primary source/code execution and review cockpit.
- GitHub #115 is the durable human-readable executive feed and cross-arm checkpoint.
- Replit and GitHub Actions are runtime/evidence surfaces only; they are not independent controllers or task queues.

## Mandatory startup
Before material work, read the latest #115 checkpoint and the matching Work ID packet, then verify this repository, branch, baseline revision, worktree, current workflow state, rollback target, lease, dependencies, and resource locks. Reject stale, duplicate, unleased, or conflicting work.

## Executor policy
- Use Mac Codex/local shell/direct GitHub/runtime tooling for source inspection, workflow review, tests, diffs, fixes, git, and evidence.
- Replit Agent is prohibited for this repository, including polling, audits, implementation, task planning, testing, or fallback work.
- One root cause equals one MASTER Work ID. Never create a second issue/task because a worker is busy or a status read fails.
- Consequential changes require builder → independent verifier/Red Team → Closer → protected release → post-change verification.

## Keepalive-specific safety
- This repository may only perform the bounded public health/readiness checks defined by its current MASTER packet.
- A health check may observe and alert; it may not restart another app, send customer communications, mutate CRM/customer data, change provider configuration, or bypass an emergency stop.
- Never add secrets, authentication tokens, private endpoints, customer information, or payment data to this public repository or its Actions logs.
- Preserve bounded cadence, timeouts, retry limits, concurrency controls, and honest failure reporting. A successful curl proves only that exact response at that time.
- Do not claim Dealflows, Roof Claims CRM, auto-calling, revenue automation, or customer workflows are healthy from this pinger alone.

## Publish and writeback
Publish/workflow activation follows the exact MASTER Work ID gate and independently verifies the workflow revision, schedule, endpoint response, cost, failure behavior, and rollback. Every material result writes back to the same Autonomy Runner Work ID, #115, the matching child issue, and the Mac Codex checkpoint.

Any bypass of MASTER, duplicate worker, unsupported completion claim, or protected effect must fail closed.